{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "echo \"no install needed — the playground is static\"",
  "buildCommand": "rm -rf public && mkdir public && cp -r index.html css js public/",
  "outputDirectory": "public"
}
